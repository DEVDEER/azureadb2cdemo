{
  "Authentication": {
    "AzureAdB2C": {
      "ClientId": "{Application-ID of App in AAD B2C}",
      "ClientSecret": "{Secret key of App in AAD B2C}",
      "Tenant": "{Name of B2C tenant}.onmicrosoft.com",
      "SignUpSignInPolicyId": "b2c_1_{B2C sign up and in policy name}",
      "ResetPasswordPolicyId": "b2c_1_{B2C password reset policy name}",
      "EditProfilePolicyId": "b2c_1_{B2C edit profile policy name}",
      "RedirectUri": "https://{URL which is configured in launchSettings.json AND B2C app}/signin-oidc",
      "ApiUrl": "https://{API Endpoint Addredd to which this app talks as a middleware}",
      "ApiScopes": "{The scope URIs which are configured in the AAD B2C for the API App}"
    }
  },
  "Logging": {
    "IncludeScopes": false,
    "LogLevel": {
      "Default": "Warning"
    }
  }
}
